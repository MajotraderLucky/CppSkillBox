# M20.5 — Практическая работа

**URL:** `homework/7deb77df-a689-43fb-b883-9ed1fedbaea5`
**Status:** submission заблокирован

## 4 задачи

### Задача 1 — Запись в ведомость учёта

Append-режим. Формат: `Name Surname DD.MM.YYYY amount`. Валидация даты через `stoi(date.substr(...))`.

### Задача 2 — Бинарные картины

Размер W×H → файл `pic.txt` со случайными 0/1.
Использовать `srand(time(nullptr))` для разных запусков.

### Задача 3 — Симуляция «Рыбалка»

`river.txt` — список видов рыб (по строке). Пользователь указывает вид →
сравниваем построчно → совпадения → дописываем в `basket.txt` (append).

### Задача 4 — Симуляция банкомата

Купюры 100, 200, 500, 1000, 2000, 5000. Max 1000 купюр. State в .bin.
Команды: `+` (наполнить случайно до max), `-` (снять кратно 100).
Выдача с купюр большего номинала.
